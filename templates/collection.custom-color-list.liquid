<div class="">
<div class="page-width">
<div class="collection-hero__inner page-width">
<div class="collection-hero__text-wrapper">
 {% if collection.handle contains 'holiday' %} 
  <h2 class="h2 collection-hero__title">
  <span class="">The Pet Collection</span>
  </h2>
  {% else %}
   <h2 class="h2 collection-hero__title">
<span class="visually-hidden">Collection: </span>{{ collection.title }}
   </h2>
  {% endif %}
  {% if collection.handle contains 'holiday-collection' %}
  <div class="collection-hero__description rte">  Pamper and protect every pup with handcrafted padded leather accessories.</div>
  {% else %}
{%- if collection.metafields.custom.collection_subtitle != blank -%}
<div class="collection-hero__description rte">{{ collection.metafields.custom.collection_subtitle }}</div>
{%- endif -%}
    {%- endif -%}
</div>
</div>
</div>
{% section 'datebased-timer' %}
{% comment %}
{% section 'collectionwithslider' %}
{% endcomment %}
{% section 'collection-template-swatches' %}
</div>